# Copia este archivo como .env y completa los valores reales.
# Nunca subir .env (o cualquier archivo con secretos) al repositorio.

# Cadena de conexión a PostgreSQL usada por Prisma.
DATABASE_URL="postgresql://user:password@localhost:5432/kevala?schema=public"
